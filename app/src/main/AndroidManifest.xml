<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lee.jeson316.mydemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activites.DesignAvtivity" />
        <activity android:name=".showmessage.ShowMessageActivity" />
        <activity
            android:name=".rxjavademo.RxJavaAvtivity"
            android:label="RxDemo"
            android:theme="@style/RxNoActionBarTheme" />
        <activity android:name=".expand.ExpandActivity" />
        <activity
            android:name=".okhttpdemo.OkhttpDemoActivity"
            android:label="@string/lable_okhttp" />
        <activity android:name=".activites.SearchViewActivity" />
        <activity android:name=".savestatus.StatusStoryActivity" />
        <activity android:name=".geoquiz.QuizActivity"></activity>
    </application>

</manifest>